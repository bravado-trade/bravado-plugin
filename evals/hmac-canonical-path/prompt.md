Write a function that builds the canonical path for signing a request to the
Bravado Data API, given a pathname and a query object. I need it for
GET /trader-analytics/traders/0xABC/positions?status=open&limit=50
